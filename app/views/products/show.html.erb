<div class="w-full max-w-7xl mx-auto py-10 px-4 sm:px-6 lg:px-8">
  <div class="mb-6 flex items-center justify-between">
    <h1 class="text-3xl font-bold text-gray-900"><%= t('products.title') %></h1>
    <div class="flex space-x-2">
      <%= link_to t('actions.edit'), edit_product_path(@product), class: "bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to t('actions.back'), products_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  </div>

  <div class="bg-white shadow overflow-hidden sm:rounded-lg">
    <div class="px-4 py-5 sm:px-6 bg-gray-50">
      <h3 class="text-lg leading-6 font-medium text-gray-900 flex items-center">
        <span class="mr-2"><%= @product.name %></span>
        <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full <%= @product.stock.to_i <= 0 ? 'bg-red-100 text-red-800' : (@product.stock.to_i < 5 ? 'bg-yellow-100 text-yellow-800' : 'bg-green-100 text-green-800') %>">
          Stock: <%= @product.stock %>
        </span>
      </h3>
      <p class="mt-1 max-w-2xl text-sm text-gray-500">SKU: <%= @product.sku %></p>
    </div>
    <div class="border-t border-gray-200">
      <dl>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500"><%= t('products.attributes.price') %></dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= format_currency(@product.price) %></dd>
        </div>
        <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500"><%= t('products.attributes.cost') %></dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= format_currency(@product.cost) if @product.cost %></dd>
        </div>
        <% if @product.cost && @product.cost > 0 %>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500"><%= t('products.attributes.profit_margin') %></dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
            <%= format_currency(@product.price - @product.cost) %>
            (<%= number_to_percentage(((@product.price - @product.cost) / @product.cost * 100), precision: 1) %>)
          </dd>
        </div>
        <% end %>
        <div class="<%= @product.cost && @product.cost > 0 ? 'bg-white' : 'bg-gray-50' %> px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500"><%= t('products.attributes.supplier') %></dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
            <%= link_to @product.supplier.name, supplier_path(@product.supplier), class: "text-blue-600 hover:text-blue-800" %>
          </dd>
        </div>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500"><%= t('products.attributes.description') %></dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @product.description %></dd>
        </div>
        <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500"><%= t('products.attributes.created_at') %></dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @product.created_at.strftime("%B %d, %Y at %I:%M %p") %></dd>
        </div>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500"><%= t('products.attributes.updated_at') %></dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @product.updated_at.strftime("%B %d, %Y at %I:%M %p") %></dd>
        </div>
      </dl>
    </div>
  </div>

  <div class="mt-6 flex justify-between">
    <div>
      <%= button_to "Delete Product", product_path(@product), method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded", form: { data: { turbo_confirm: "Are you sure you want to delete this product?" } } %>
    </div>
  </div>
</div>
