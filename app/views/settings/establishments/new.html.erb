<% content_for :title, t("sar.establishments.new") %>

<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-2xl font-bold text-gray-900 mb-6"><%= t("sar.establishments.new") %></h1>
  <%= render "form", establishment: @establishment %>
</div>
