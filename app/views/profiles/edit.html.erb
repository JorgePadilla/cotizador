<%= render HeaderComponent.new(title: t('users.edit_profile')) %>

<div class="bg-white shadow rounded-lg p-6 mb-6">
  <%= form_with model: @user, url: profile_path, method: :patch do |form| %>
    <% if @user.errors.any? %>
      <div class="bg-red-50 text-red-500 p-4 rounded-lg mb-6">
        <h2 class="text-lg font-medium mb-2"><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
        <ul class="list-disc pl-5">
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="grid grid-cols-1 gap-6">
      <div>
        <%= form.label :name, t('users.attributes.name'), class: "block text-sm font-medium text-gray-700" %>
        <%= form.text_field :name, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
      </div>

      <div>
        <%= form.label :email_address, t('users.attributes.email'), class: "block text-sm font-medium text-gray-700" %>
        <%= form.email_field :email_address, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
      </div>

      <div>
        <%= form.label :language, t('users.attributes.language'), class: "block text-sm font-medium text-gray-700" %>
        <%= form.select :language, 
            options_for_select([
              [t('languages.en'), 'en'],
              [t('languages.es'), 'es']
            ], @user.language),
            {}, 
            { class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" } 
        %>
      </div>

      <div>
        <%= form.label :default_tax, t('users.attributes.default_tax', default: 'Default Tax Rate (%)'), class: "block text-sm font-medium text-gray-700" %>
        <% tax_value = @user.default_tax.present? ? (@user.default_tax * 100).round(2) : 15 %>
        <div class="mt-1 flex rounded-md shadow-sm">
          <%= form.number_field :default_tax, 
                               value: tax_value,
                               step: "0.01", 
                               min: "0", 
                               max: "100", 
                               class: "flex-1 min-w-0 block w-full rounded-none rounded-l-md border-gray-300 focus:border-blue-500 focus:ring-blue-500" %>
          <span class="inline-flex items-center px-3 rounded-r-md border border-l-0 border-gray-300 bg-gray-50 text-gray-500 text-sm">
            %
          </span>
        </div>
        <p class="mt-1 text-sm text-gray-500"><%= t('users.hints.default_tax', default: 'Enter percentage value (e.g., 15 for 15%)') %></p>
      </div>

      <div class="flex items-center justify-between pt-4 border-t border-gray-200">
        <%= link_to t('actions.cancel'), profile_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        
        <%= form.submit t('actions.save'), class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      </div>
    </div>
  <% end %>
</div>
